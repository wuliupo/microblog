<% if (!user) { %>
<div class="hero-unit">
    <h1>欢迎来到 Microblog</h1>
    <p>Microblog 是一个基于 Node.js 的微博系统。</p>
    <p>
        <a class="btn btn-primary btn-large" href="/login" data-toggle="modal" data-target="#loginModal">登录</a>
        <a class="btn btn-large" href="/reg" data-toggle="modal" data-target="#regModal">立即注册</a>
    </p>
</div>
<% } else { %>
<%- partial('say') %>
<% } %>
<%- partial('posts', {posts:posts}) %>